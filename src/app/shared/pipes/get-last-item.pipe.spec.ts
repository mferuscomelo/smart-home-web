import { GetLastItemPipe } from './get-last-item.pipe';

describe('GetLastItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLastItemPipe();
    expect(pipe).toBeTruthy();
  });
});
